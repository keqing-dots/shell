theme[main_bg]="${BASE}"
theme[main_fg]="${TEXT}"
theme[title]="${LAVENDER}"
theme[hi_fg]="${ACCENT_ALT}"
theme[selected_bg]="${ACCENT_CONTAINER}"
theme[selected_fg]="${TEXT}"
theme[inactive_fg]="${TEXT_MUTED}"
theme[proc_misc]="${ACCENT}"
theme[cpu_box]="${SURFACE_ALT}"
theme[mem_box]="${SURFACE_ALT}"
theme[net_box]="${SURFACE_ALT}"
theme[proc_box]="${SURFACE_ALT}"
theme[div_line]="${SURFACE_ALT}"

theme[temp_start]="${ACCENT}"
theme[temp_mid]="${LAVENDER}"
theme[temp_end]="${ACCENT_ALT}"
theme[cpu_start]="${ACCENT}"
theme[cpu_mid]="${LAVENDER}"
theme[cpu_end]="${ACCENT_ALT}"
theme[free_start]="${ACCENT}"
theme[free_mid]="${LAVENDER}"
theme[free_end]="${ACCENT_ALT}"
theme[cached_start]="${ACCENT}"
theme[cached_mid]="${LAVENDER}"
theme[cached_end]="${ACCENT_ALT}"
theme[available_start]="${ACCENT}"
theme[available_mid]="${LAVENDER}"
theme[available_end]="${ACCENT_ALT}"
theme[used_start]="${ACCENT}"
theme[used_mid]="${LAVENDER}"
theme[used_end]="${ACCENT_ALT}"
theme[download_start]="${ACCENT}"
theme[download_mid]="${LAVENDER}"
theme[download_end]="${ACCENT_ALT}"
theme[upload_start]="${ACCENT}"
theme[upload_mid]="${LAVENDER}"
theme[upload_end]="${ACCENT_ALT}"
